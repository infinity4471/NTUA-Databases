/**
 * Enables the modification of the Button appearance.
 *
 * The available styling options are:
 * - `default`
 * - `flat`
 * - `bare`
 * - `outline`
 *
 * @example
 * ```ts-no-run
 * <Button look="bare"></Button>
 * ```
 */
export declare type ButtonLook = 'default' | 'bare' | 'flat' | 'outline';
